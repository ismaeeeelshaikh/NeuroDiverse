// If you want to keep the ChatBot card but remove the floating widget,
// modify this component to remove the floating button

export default function ChatWidget() {
  return (
    // Remove the floating button/icon code
    <></>
  );
}